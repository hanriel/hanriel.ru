<!DOCTYPE html>
<html class="page" lang="ru">
<head>
    {% if page.url == '/' %}
        {% set isIndex = true %}
    {% else %}
        {% set titleSuffix = ' — Hanriel' %}
        {% set isIndex = false %}
    {% endif %}

    <title>
        {{ renderData.title or title }}{{ titleSuffix }}
    </title>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ preview or site.description }}">
    <meta name="theme-color" content="#FF9900">

    {% include 'open-graph.njk' %}
    {% include 'icons.njk' %}

    <link rel="alternate" href="{{ site.paths.articlesFeed }}" type="application/rss+xml" title="Статьи — Hanriel">
    <link rel="manifest" href="/manifest.json">
    <link rel="stylesheet" href="/styles/styles.css">
    <link rel="stylesheet" href="/styles/print.css" media="print">
</head>
<body class="page__body">
    {% include 'header.njk' %}

    {{ content | safe }}

    <script type="module" src="/scripts/index.js"></script>
</body>
</html>
